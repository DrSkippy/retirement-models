---
title: Lint Report 2026-06-14
tags: [lint, maintenance]
sources: []
updated: 2026-06-14
---

# Lint Report — 2026-06-14

## Summary
- 🔴 Errors: 1
- 🟡 Warnings: 2
- 🔵 Info: 3

---

## 🔴 Broken Links

- **[[overview]]** references `[[db-persistence]]` — no page `wiki/pages/db-persistence.md` exists.
  - Context: open question "How does the DB persistence layer relate to the CLI `--save-db` flag? (See [[db-persistence]])"
  - Fix: create `[[db-persistence]]` by ingesting `models/db.py`, or remove the reference and leave it as a plain open question.

---

## 🟡 Warnings

### Orphan Pages

- **[[simulation-flow]]** — has zero inbound `[[slug]]` links from any content page. It appears in `index.md` under both Architecture and Flows, but no other wiki page links to it.
  - Fix: add `[[simulation-flow]]` to the Related sections of `[[simulation-engine]]`, `[[asset-models]]`, `[[tax-model]]`, and `[[monte-carlo]]`. (See Missing Cross-References below for the exact list.)

### Inaccurate Description

- **[[asset-models]]** — the Equity `_setup()` section says "Annual rates divided by 12 for monthly compounding" but `growth_rate_volatility` is **not** divided by 12 in the code (`self.growth_rate_volatility = self.appreciation_rate_volatility` directly). The description implies all rates are converted monthly, which is misleading.
  - Affected claim: the config example shows `"appreciation_rate_volatility": 0.02`, which is used directly as the monthly std dev passed to `np.random.normal()`.
  - Fix: add a note clarifying that `growth_rate` is divided by 12 but `growth_rate_volatility` is used as-is (i.e., the config value is interpreted as monthly std dev, not annual).

---

## 🔵 Info

### Missing Concept Page

- **`[[db-persistence]]`** — referenced once in `[[overview]]` (open questions) and nowhere else. `models/db.py` (SQLAlchemy persistence layer, PostgreSQL, `--save-db` flag integration) is not documented in any wiki page.
  - Suggestion: ingest `models/db.py` and `migrations/001_initial_schema.sql` to create a `db-persistence` page. This would also close the open question in `[[overview]]`.

### Missing Cross-References

These content pages discuss parts of the simulation flow but don't link to `[[simulation-flow]]`, leaving it an orphan:

| Page | Should add |
|---|---|
| `[[simulation-engine]]` | `[[simulation-flow]]` in Related section |
| `[[asset-models]]` | `[[simulation-flow]]` in Related section |
| `[[tax-model]]` | `[[simulation-flow]]` in Related section |
| `[[monte-carlo]]` | `[[simulation-flow]]` in Related section |

### Coverage Gap

- Open question from `[[overview]]`: *"Is there an inflation adjustment applied to expenses? The `inflation_rate` field is in `WorldConfig` but its application in the simulation loop is not immediately visible."*
  - Suggestion: grep `models/scenarios.py` and `models/expenses.py` for `inflation_rate` usage; if unused in the loop, document it as a known gap in `[[simulation-engine]]` and `[[configuration]]`.
